---
const { label, outlined, customClass, url, ariaLabel, role, target, downloadable, fileName = "Pinier_Nicolas_cv" } = Astro.props;
---
<a href={url} {...(downloadable && { download: fileName })}
   target={target} aria-label={ariaLabel} role={role}
   class=`text center justify-center flex items-center gap-4 rounded-xl py-2 px-4 duration-300  ${outlined ? 'transition-colors border border-primary text-primary lg:hover:bg-primary lg:hover:text-white' : 'transition-opacity bg-primary lg:hover:opacity-80 text-white'} ${customClass ? customClass : ''}`
>
    { label }
    <slot />
</a>
