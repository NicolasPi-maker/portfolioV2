---
const { label, outlined, customClass } = Astro.props;
---
<button type="button" class=`rounded-xl py-2 px-6 ${outlined ? 'border border-primary text-primary' : 'bg-primary text-white'} ${customClass ? customClass : ''}` title={label} >
    { label }
</button>


