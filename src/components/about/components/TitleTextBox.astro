---
const { title, text, titleFontSize = 'text-5xl md:text-6xl' } = Astro.props;
---

<div class="flex flex-col gap-2 center h-full p-4">
    <h2 class=`text-gradient ${titleFontSize}`>{ title }</h2>
    <p class=" text-center">{ text }</p>
</div>


